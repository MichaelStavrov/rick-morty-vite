export enum RoutesMap {
  home = '/',
  locations = '/locations',
  location = ':id',
  heroes = '/heroes',
  hero = ':id',
  episodes = '/episodes',
  episode = ':id',
  signIn = '/signin',
  notFound = '/*',
}
